# Nubela Assignment - Vincent

- if you can't run it via docker, please try it using these steps:
    - run `python server.py ./socket.sock` in 1st terminal
    - run `python client.py` in 2nd terminal